class Solution {
public:
    vector<int> buildArray(vector<int>& nums) {
        vector<int> permutation;
        int n = nums.size();
        for(int i=0;i<n;i++) permutation.push_back(nums[nums[i]]);
        return permutation;
    }
};
